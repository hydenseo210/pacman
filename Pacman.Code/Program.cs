namespace Pacman.Code;

public static class Program
{
    public static void Main(string[] args)
    {
         // var currentGameState = GameDownload.DownloadMapFromFile("../../../Assets/Maps/LevelOneMapTest.txt");
         // var gameStateQueue = new Queue<GameState>();
         // var nextGameState = GameDownload.DownloadMapFromFile("../../../Assets/Maps/LevelTwoMap.txt");
         // gameStateQueue.Enqueue(nextGameState);
         // var console = new ConsoleWrapper();
         // var pacmanController = new PacmanController();
         // var ghostController = new GhostController(new Blinky(new AggressiveBehaviour()), new Pinky(new AggressiveBehaviour()));
         // var game = new Game(currentGameState, gameStateQueue, pacmanController, ghostController,console, new ThreadSleeper());
         // GameController.Run(game, console, new ThreadSleeper());

    }
}